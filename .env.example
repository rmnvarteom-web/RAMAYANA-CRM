# Copy this file to .env and fill in real values. Never commit .env itself.

# PostgreSQL connection string (Neon, local, etc.)
DATABASE_URL="postgresql://user:password@localhost:5432/ramayana_crm?schema=public"
